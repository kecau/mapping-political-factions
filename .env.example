# Copy to .env and fill in. Never commit the filled-in file.
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=

# Optional overrides for where the package reads and writes.
# MPF_CACHE=./cache
# MPF_FIGURES=./figures
# MPF_TABLES=./tables
